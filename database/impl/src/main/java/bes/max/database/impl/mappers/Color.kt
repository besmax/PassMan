package bes.max.database.impl.mappers

//fun AndroidColor.toComposeColor(): ComposeColor {
//    return ComposeColor(red = red / 255f, green = green / 255f, blue = blue / 255f, alpha = alpha / 255f)
//}
//
//fun ComposeColor.toAndroidColor(): Int {
//    return AndroidColor.argb(
//        (alpha * 255).toInt(),
//        (red * 255).toInt(),
//        (green * 255).toInt(),
//        (blue * 255).toInt()
//    )
//}